name: git_stamp
description: "A tool to generate a git history that you can see when you run the application."
version: 1.0.0
homepage:
environment:
  sdk: '>=3.2.3 <4.0.0'
  flutter: ">=1.17.0"
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_test:
    sdk: flutter
flutter: